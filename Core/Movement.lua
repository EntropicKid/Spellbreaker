-- ============================================================
-- Core/Movement.lua — ШАГОМЕР И ЛИМИТ ПЕРЕДВИЖЕНИЯ ЗА ХОД
--
-- ЗАЧЕМ. В настольной системе ход состоит из двух вещей: персонаж
-- перемещается и что-то делает. Аддон до сих пор знал только вторую
-- половину — заклинание тратило ход, а перемещение не стоило ничего, и
-- «отбежал на тридцать метров и ударил» было ровно так же дёшево, как
-- «ударил, стоя на месте». Дистанции у заклинаний при этом есть и
-- проверяются (см. SB.Logic.IsSpellInRange), то есть расстояние в
-- системе значит много, а движение — ничего.
--
-- КАК СЧИТАЕТСЯ. Пройденный путь берётся ИНТЕГРИРОВАНИЕМ СКОРОСТИ:
-- каждый кадр к счётчику прибавляется GetUnitSpeed("player") * elapsed.
--
-- Это единственный способ, который работает везде. Очевидные
-- альтернативы не годятся:
--   • UnitPosition("player") — Blizzard режет его в подземельях,
--     на аренах и полях боя: возвращает nil, и шагомер там просто
--     перестал бы считать;
--   • C_Map.GetPlayerMapPosition — то же ограничение плюс нормированные
--     0..1 координаты, которые нужно переводить в ярды по размеру карты,
--     а размер известен не для всех карт.
-- GetUnitSpeed не ограничен ничем, отдаёт ярды в секунду и одинаково
-- честно считает бег, ходьбу, полёт и плавание.
--
-- КОГО ВЕЗУТ — ТОТ ТОЖЕ ПЕРЕМЕЩАЕТСЯ, и это единственное место, где
-- GetUnitSpeed("player") врёт. Персонаж, севший верхом на СУЩЕСТВО
-- (повозка, конь-НПС, чужой транспорт), сам не бежит: собственная его
-- скорость ровно ноль, а по миру он при этом едет. Шагомер молчал, и
-- верхом можно было пересечь всю сцену бесплатно.
--
-- Ловим это двумя способами, и второй нужен именно на приватном
-- сервере:
--   • штатный транспорт (UnitInVehicle) — спрашиваем скорость у того,
--     кто везёт, у юнита "vehicle";
--   • всё остальное — сверяем МИРОВЫЕ КООРДИНАТЫ раз в NOTIFY_INTERVAL.
--     Сервер может усадить персонажа так, что игровой API транспорта об
--     этом не знает вовсе (у эмуляторов это обычное дело), а вот
--     координаты игрока едут вместе с тем, кто его везёт, всегда.
--
-- Координаты остаются ЗАПАСНЫМ путём, а не основным: они молчат в
-- подземельях и на полях боя (см. выше), и точность у них в пять раз
-- грубее. Основным по-прежнему остаётся скорость.
--
-- ЕДИНИЦЫ. GetUnitSpeed отдаёт ЯРДЫ, а вся система говорит в МЕТРАХ
-- (у заклинаний distance = 2.5 / 8.5 / 19). Переводим на месте, тем же
-- коэффициентом, что и проверка дальности в Core/Logic.lua.
--
-- ============================================================
-- ХОДИШЬ ТОЛЬКО В СВОЙ ХОД
--
-- ЖАЛОБА, С КОТОРОЙ ПРАВИЛО ПЕРЕПИСАНО: боец ближнего боя в пошаговом
-- режиме не может никого догнать — даже рывком. Разбираться стоит не с
-- числом метров, а с тем, КОГДА их разрешено тратить.
--
-- Прежде запас был общим на круг и тратился когда угодно: и в свой ход,
-- и в чужие. Значит убегать можно было НЕПРЕРЫВНО — заклинатель отходил
-- в каждый чужой ход по чуть-чуть, а догоняющий тратил своё на подход и
-- приходил к цели с пустыми ногами. Хуже того, первый круг давал вдвое:
-- счётчик пуст на входе в режим, маг уходит на двадцать метров ВНЕ
-- своего хода, бьёт, счётчик обнуляется — и уходит ещё на двадцать.
--
-- ТЕПЕРЬ ОКНО ПЕРЕДВИЖЕНИЯ — ЭТО СВОЙ ХОД, И ТОЛЬКО ОН:
--
--   1. Пошаговый режим включился — ENTRY_GRACE секунд все ходят
--      свободно и бесплатно: это время заметить объявление и
--      остановиться. По истечении счётчик запирается у всех.
--   2. Дошла очередь — счётчик обнуляется, и весь предел твой. Это
--      единственное окно, в котором персонаж ходит.
--   3. Походил (или пропустил) — счётчик снова полон, движения нет до
--      следующего своего хода.
--
-- ПОЧЕМУ ЭТО И ЕСТЬ ОТВЕТ НА ЖАЛОБУ. Убегать теперь можно ровно один
-- раз за круг и ровно на свой предел — как и догонять. Расстояние
-- перестало восстанавливаться быстрее, чем сокращается, и ближний бой
-- больше не зависит от согласия цели.
--
-- ЗАПЕРТЫЙ СЧЁТЧИК ДЕЙСТВОВАТЬ НЕ МЕШАЕТ. Это отличает его от честного
-- упора в предел: вне своего хода действовать и так нельзя (этим
-- заведует очередь), и вешать сверху второй запрет незачем — он
-- сработал бы ровно в тот миг, когда ход наконец дойдёт.
--
-- ЧЕМ ЗАМОК ДЕРЖИТСЯ. Аддон не может отнять у игрока клавиши — значит
-- шаг вне своего хода возможен физически. Стоит он ровно столько же,
-- сколько любой метр сверх предела: усталостью (см.
-- SB.Movement.AddOverrun). Без этой цены правило было бы украшением.
--
-- «ВСЕ СРАЗУ» ЭТОГО НЕ ЗНАЕТ. Там нет очереди, а значит нет и «своего
-- хода», который можно открыть: запереть пришлось бы весь круг целиком.
-- В этом режиме черта прежняя — конец своего хода
-- (см. SB.Movement.NoteTurnClosed).
--
-- ХОД БОЛЬШЕ НЕ ПРОПУСКАЕТСЯ САМ. Выбранный запас закрывал ход
-- автоматически, и при запертом счётчике это означало бы, что пошаговый
-- режим пропускает ходы всем и каждый круг. Правило снято целиком:
-- упор в предел запрещает действие — этого достаточно, а закрывать ход
-- за игрока незачем.
--
-- ПРЕДМЕТ — БОНУСНОЕ действие и предела не касается: выпить зелье можно
-- и упёршись в него (см. SB.Logic.IsBonusAction).
-- ============================================================
local addonName, SB = ...
SB.Movement = SB.Movement or {}

-- Тот же коэффициент, что в SB.Logic.IsSpellInRange.
local YARDS_TO_METERS = 0.9144

-- Как часто шлём MOVEMENT_CHANGED. Само накопление идёт КАЖДЫЙ кадр
-- (иначе остановка в середине интервала теряется и путь занижается), а
-- вот перерисовывать шапку 60 раз в секунду незачем.
local NOTIFY_INTERVAL = 0.2

-- Сколько секунд после включения пошагового режима все ходят свободно.
-- Объявление «Пошаговый режим» игрок должен успеть прочитать и
-- остановиться; шаги, сделанные по инерции в эти секунды, не стоят
-- ничего и в счётчик не идут. По истечении счётчик запирается.
local ENTRY_GRACE = 2
SB.Movement.ENTRY_GRACE = ENTRY_GRACE

-- «Предела нет вовсе» — отдельное значение, а НЕ ноль.
--
-- Сначала ноль и означал «ограничение снято», и это было ошибкой:
-- эффекты умеют двигать кап (канал movePct), и достаточно сильный
-- дебафф на замедление обнулял бы предел — то есть СНИМАЛ ограничение
-- вместо того, чтобы обездвижить. Теперь ноль честно значит ноль
-- («шагнул — и действовать уже нечем»), а снятие предела задаётся
-- отдельным значением, до которого арифметике не дотянуться.
local NO_LIMIT = -1
SB.Movement.NO_LIMIT = NO_LIMIT

local function db()
    return SpellbreakerCharDB
end

-- ============================================================
-- ЧТЕНИЕ И ЗАПИСЬ
-- ============================================================

--- МЕТРЫ ДЛЯ ПОКАЗА — ДО ДЕСЯТОЙ. Целые скрывали, сколько на самом деле
--- набежало: 2.5 м показывались тройкой, и шаг в полметра выглядел как
--- скачок на метр. Лишний «.0» у целых отбрасываем: «5.3/27», а не
--- «5.3/27.0».
--- @return string
function SB.Movement.FormatMeters(v)
    local s = string.format("%.1f", tonumber(v) or 0)
    return (s:gsub("%.0$", ""))
end

-- ============================================================
-- ЗАМОК ВНЕ СВОЕГО ХОДА
--
-- Отдельный флаг, а не «дописать moveDistance до предела», и причин
-- две. Предел ходит: «Спринт» его поднимает, замедление роняет, Ведущий
-- переписывает, — и записанное число разошлось бы с ним в тот же миг.
-- Вторая важнее: замок и упор ОТВЕЧАЮТ ПО-РАЗНОМУ на вопрос «можно ли
-- действовать» (см. BlocksAction), и по одному числу их было бы не
-- отличить.
--
-- Флаг лежит в сохранёнке рядом с путём: /reload в чужой ход не должен
-- выдавать метры, которых у персонажа сейчас нет.
-- ============================================================

-- ОКНО НА ВХОДЕ В РЕЖИМ — в памяти, а не в сохранёнке. Это две секунды
-- одной сцены, и переживать /reload им незачем.
--
-- ДВА ЗАСОВА, А НЕ ОДИН: флаг закрывает таймер, но и крайний срок
-- проверяется сам. Потеряйся таймер (ошибка в чужом обработчике,
-- выгрузка кадра) — окно осталось бы открытым навсегда, то есть замок
-- не сработал бы ни разу и правило молча выключилось бы целиком.
local entryGrace  = false
local graceUntil  = 0

--- Идут ли те самые секунды свободного хода после включения режима.
function SB.Movement.InEntryGrace()
    if not entryGrace then return false end
    if GetTime and GetTime() >= graceUntil then return false end
    return true
end

-- ============================================================
-- ДВА ВОПРОСА, А НЕ ОДИН: «ЗАПЕРТО ЛИ» И «ЧЕЙ ХОД»
--
-- Вопроса здесь действительно два, и путать их нельзя.
--
--   IsTurnPinned — ЧЕЙ СЕЙЧАС ХОД. Отвечает по очереди и только по ней:
--                  мой черёд открыт или нет. Окна на входе не знает.
--   IsPinned     — МОЖНО ЛИ ИДТИ ПРЯМО СЕЙЧАС. Это то же самое ПЛЮС
--                  окно на входе, в которое ходят все.
--
-- ЧТО СЛОМАЛОСЬ, ПОКА ВОПРОС БЫЛ ОДИН. Очередь сверяет своё состояние
-- с сохранённым флагом (см. NotifyTransitions в Core/TurnOrder.lua):
-- «надо запереть, а не заперто — запри; не надо, а заперто — отопри».
-- Сверяла она через IsPinned, а тот в окне отвечает «не заперто»
-- ВСЕГДА. Значит в эти две секунды очередь могла флаг ТОЛЬКО ПОСТАВИТЬ
-- и никогда не снять.
--
-- Наружу это выглядело так: у первого ходящего через две секунды после
-- начала боя пропадало всё передвижение. Пакетов состояния в первые
-- секунды приходит несколько — объявление, сборка слотов, пролистывание
-- павших, — и достаточно одного, в котором черёд ещё не дошёл, чтобы
-- флаг встал. Следующий пакет, уже верный, снять его не мог: IsPinned
-- в окне молчит. Окно кончалось — и первый ходящий встречал свой ход
-- запертым, то есть не мог сократить дистанцию ни на метр.
-- ============================================================

--- Закрыт ли мой черёд по ОЧЕРЕДИ — без оглядки на окно входа.
--- Это спрашивает сама очередь, когда сверяет состояние с флагом.
--- @return boolean
function SB.Movement.IsTurnPinned()
    local d = db()
    if not (d and d.movePinned) then return false end
    -- Режим мог смениться, пока флаг лежал в сохранёнке: «все сразу»
    -- своих ходов не знает, и запирать там нечего.
    local TO = SB.TurnOrder
    if not (TO and TO.IsActive and TO.IsActive()) then return false end
    if TO.GetMode and TO.GetMode() == "all" then return false end
    return true
end

--- Заперт ли счётчик прямо сейчас — то есть идёт ЧУЖОЙ ход.
--- Это спрашивают правила передвижения и подписи.
--- @return boolean
function SB.Movement.IsPinned()
    -- Окно на входе сильнее замка: в эти секунды ходят все.
    if SB.Movement.InEntryGrace() then return false end
    return SB.Movement.IsTurnPinned()
end

--- Свой ход закрыт (или ещё не начался): счётчик заперт до своего хода.
function SB.Movement.PinTurn()
    local d = db()
    if not d or d.movePinned then return end
    d.movePinned = true
    SB.Events.Fire(SB.E.MOVEMENT_CHANGED)
end

--- Дошла очередь: замок снят, счётчик обнулён, весь предел твой.
function SB.Movement.FillBudget()
    local d = db()
    if not d then return end
    d.movePinned = nil
    SB.Movement.ResetDistance()
    SB.Events.Fire(SB.E.MOVEMENT_CHANGED)
end

--- Открыть окно свободного хода на ENTRY_GRACE секунд.
---
--- ВХОД В РЕЖИМ — ЭТО НЕ КОМАНДА «ЗАМРИ». Персонаж в этот миг куда-то
--- шёл, и объявление надо успеть прочитать; метры этих секунд не идут
--- ни в счётчик, ни в усталость (см. ShouldCount). По истечении окна
--- счётчик стирается — прощая всё пройденное — и замок начинает
--- действовать по обычному правилу.
function SB.Movement.StartEntryGrace()
    entryGrace = true
    graceUntil = (GetTime and GetTime() or 0) + ENTRY_GRACE
    SB.Movement.ResetDistance()
    SB.Events.Fire(SB.E.MOVEMENT_CHANGED)
    if C_Timer then
        C_Timer.After(ENTRY_GRACE, SB.Movement.EndEntryGrace)
    else
        SB.Movement.EndEntryGrace()
    end
end

--- Закрыть окно досрочно. Отдельной функцией, а не замыканием внутри
--- StartEntryGrace: прогону нужно закрыть ИМЕННО ОКНО, ничего больше не
--- трогая. Пока закрытие жило в таймере, прогон крутил все таймеры
--- разом — вместе с таймером хода, который тут же уводил очередь
--- дальше, — и проверка мерила не то, что собиралась.
function SB.Movement.EndEntryGrace()
    if not entryGrace then return end
    entryGrace, graceUntil = false, 0
    -- Стираем ПОСЛЕ окна, а не до: метры этих секунд прощаются
    -- целиком, иначе они съели бы первый же свой ход.
    SB.Movement.ResetDistance()
    SB.Events.Fire(SB.E.MOVEMENT_CHANGED)
end

--- Сколько метров персонаж уже прошёл в текущем ходу.
--- Значение ЗАЖАТО капом: копить путь сверх предела бессмысленно (упор
--- есть упор, «прошёл сто метров» и «прошёл двенадцать» запрещают одно и
--- то же), а на экране 118/12 выглядело бы просто сломанным счётчиком.
function SB.Movement.GetDistance()
    local d = db()
    local walked = (d and tonumber(d.moveDistance)) or 0
    local cap = SB.Movement.GetCap()
    -- ЗАМОК ЧИТАЕТСЯ ПОЛНЫМ СЧЁТЧИКОМ, а не нулём: на экране он значит
    -- ровно то же самое, что выхоженный предел, — «идти нечем». Разница
    -- между ними только в праве действовать, и она объясняется словами
    -- в подсказке, а не числом в бейдже.
    if cap ~= NO_LIMIT and SB.Movement.IsPinned() then return cap end
    if cap ~= NO_LIMIT and walked > cap then return cap end
    return walked
end

--- Личный кап передвижения за ход, в метрах.
--- Хранится в персонаже (Ведущий может выдать больше или меньше), но по
--- умолчанию берётся из конфига, навыка и «мягких» профилей расы и
--- класса — тем же способом, что максимум здоровья и ресурса.
---
--- Висящие эффекты двигают кап в ЛЮБОМ случае, и до, и после ГМ-правки:
--- «Спринт» должен ускорять и того, кому Ведущий выдал свой предел, —
--- иначе персональный предел молча отключал бы всю магию скорости.
--- @return number  метры, либо SB.Movement.NO_LIMIT, если предела нет
function SB.Movement.GetCap()
    local d = db()
    local stored = d and tonumber(d.moveCap)

    -- Предел, снятый Ведущим, эффекты уже не двигают: двигать нечего.
    if stored and stored < 0 then return NO_LIMIT end

    local base = stored or SB.Movement.GetDefaultCap()

    -- ЭФФЕКТЫ ДВИГАЮТ ПРЕДЕЛ В ПРОЦЕНТАХ, а не в метрах, и считаются они
    -- от УЖЕ СОБРАННОГО предела — вместе с «Атлетикой», расой и классом.
    --
    -- Раньше канал был метровым, и одно и то же замедление значило
    -- разное: −6 м отнимало половину хода у обычного персонажа и
    -- четверть у таурена-разбойника с развитой «Атлетикой». То есть
    -- замедление тем слабее, чем быстрее цель, — ровно наоборот тому,
    -- зачем его вешают. Процент бьёт одинаково по всем.
    local slowed = base
    if SB.ActiveEffects and SB.ActiveEffects.GetMod then
        local pct = SB.ActiveEffects.GetMod("movePct")
        if pct ~= 0 then
            -- Округляем к ближайшему: половина от 15 — это 8 метров, а
            -- не 7.5, и в шапке должно стоять целое число.
            slowed = math.floor(base * (1 + pct / 100) + 0.5)
        end
    end

    -- ЗАМЕДЛЕНИЕ НЕ ОБЕЗДВИЖИВАЕТ ПОЛНОСТЬЮ. Предел в ноль означает, что
    -- персонаж упёрся, ещё не сделав шага, — то есть не может ни
    -- применить способность, ни сделать что-либо кроме пропуска хода
    -- (см. IsExhausted и SB.Logic.CanCastNow). Два-три сложившихся
    -- замедления доводили до этого сами собой, и игрок выпадал из сцены
    -- без единого броска на спасение: его ход просто уходил дальше.
    --
    -- Поэтому снизу стоит Config.MoveCapMin: сколько бы ни сложилось
    -- помех, шаг остаётся. Это не поблажка — это разница между «медленно»
    -- и «выбыл».
    --
    -- ГМ-ПРАВКУ ПОЛ НЕ ПОДНИМАЕТ. Если Ведущий выдал персонажу предел
    -- меньше минимума (в том числе ноль — «связан, лежит, вморожен»),
    -- это решение сцены, а не побочный итог арифметики, и переигрывать
    -- его нечем. Пол не может поднять предел выше того, что было ДО
    -- замедления.
    local floorCap = math.min(base, tonumber(SB.Data.Config.MoveCapMin) or 3)

    -- Ниже нуля не уходим, иначе значение столкнулось бы с NO_LIMIT и
    -- замедление внезапно превратилось бы в свободу передвижения.
    return math.max(0, floorCap, slowed)
end

--- Есть ли вообще предел (false — Ведущий его снял).
function SB.Movement.HasLimit()
    return SB.Movement.GetCap() ~= NO_LIMIT
end

--- Кап «по умолчанию» — конфиг, навык и профили, без персональной
--- правки Ведущего и без висящих эффектов.
function SB.Movement.GetDefaultCap()
    local base = tonumber(SB.Data.Config and SB.Data.Config.MoveCap) or 15
    -- «Атлетика» — метр за вложенное очко, в обе стороны (см. Core/Skills.lua).
    if SB.Skills and SB.Skills.GetAthleticsMoveBonus then
        base = base + SB.Skills.GetAthleticsMoveBonus()
    end
    base = base + (SB.Data.GetSoftBonus and SB.Data.GetSoftBonus("moveCap") or 0)
    -- ПОДАВЛЕННАЯ «АТЛЕТИКА» НЕ ОБЕЗДВИЖИВАЕТ — тот же пол, что держит
    -- замедление (Config.MoveCapMin, см. SB.Movement.GetCap). Штраф навыка
    -- появился вместе с метром за очко, и без пола достаточно тяжёлый
    -- дебафф на навык снова выключал бы персонажа из сцены.
    local floor = tonumber(SB.Data.Config and SB.Data.Config.MoveCapMin) or 3
    return math.max(floor, base)
end

--- Сколько метров ещё можно пройти до упора (0, если уже упёрся).
--- Без предела возвращает NO_LIMIT — вызывающему полагается это отличить.
--- В чужой ход — ноль: окно передвижения сейчас не ваше (см. IsPinned).
function SB.Movement.GetRemaining()
    local cap = SB.Movement.GetCap()
    if cap == NO_LIMIT then return NO_LIMIT end
    return math.max(0, cap - SB.Movement.GetDistance())
end

--- ЗАПАС В НОГАХ — сколько метров НЕ ВЫХОЖЕНО, без оглядки на замок.
---
--- Отличается от GetRemaining ровно вне своего хода, и разница
--- смысловая. GetRemaining отвечает «сколько можно пройти прямо
--- сейчас» — в чужой ход ноль, потому что окно чужое. Здесь вопрос
--- другой: «сколько бега в человеке осталось», и замок на него не
--- влияет — он про очередь, а не про усталость.
---
--- Спрашивает это побег (SB.Logic.GetFleeOdds). В свой ход оба ответа
--- совпадают, но бросок могут дать и вне очереди — решением Ведущего
--- или будущим правилом, — и тогда прибавка «не потратил метры — легче
--- оторваться» обнулилась бы молча.
--- @return number  метры, либо NO_LIMIT
function SB.Movement.GetReserve()
    local cap = SB.Movement.GetCap()
    if cap == NO_LIMIT then return NO_LIMIT end
    local d = db()
    local walked = (d and tonumber(d.moveDistance)) or 0
    return math.max(0, cap - walked)
end

--- ДЕЙСТВУЕТ ЛИ ПРЕДЕЛ НА ЭТОГО ПЕРСОНАЖА ПРЯМО СЕЙЧАС.
---
--- Две причины, по которым не действует, и обе одинаково полные: метры
--- по-прежнему считаются и видны в шапке, но упор в них ничего не
--- запрещает и ничего не стоит.
---
---   • Ведущий снял предел на сцену (см. TO.SetMoveFree) — общее решение
---     на всех: погоня, отступление по сюжету;
---   • персонаж СБЕЖАЛ ИЗ БОЯ (см. PM.HasFled) — личное и очевидное:
---     очередь его уже не ждёт, и держать беглеца на поводке в двенадцать
---     метров бессмысленно. Хуже того — с включённой усталостью это
---     означало бы, что убегающий умирает от бега, то есть побег
---     оказывается опаснее, чем остаться и драться.
--- @return boolean
function SB.Movement.IsUnlimited()
    if SB.TurnOrder and SB.TurnOrder.IsMoveFree and SB.TurnOrder.IsMoveFree() then
        return true
    end
    if SB.PlayerModel and SB.PlayerModel.HasFled and SB.PlayerModel.HasFled() then
        return true
    end
    return false
end

--- Выбран ли лимит передвижения. Именно это спрашивает ConfirmCast,
--- прежде чем пустить заклинание.
function SB.Movement.IsExhausted()
    if SB.Movement.IsUnlimited() then return false end
    local cap = SB.Movement.GetCap()
    if cap == NO_LIMIT then return false end
    -- Замок чужого хода — тот же нулевой запас, и счётчик обязан
    -- краснеть одинаково: идти нечем в обоих случаях.
    if SB.Movement.IsPinned() then return true end
    -- cap == 0 — полное обездвиживание: 0 >= 0, значит упёрся сразу, ещё
    -- не сделав шага. Так и задумано; см. NO_LIMIT выше о том, почему
    -- ноль больше не путается со «снятым пределом».
    return SB.Movement.GetDistance() >= cap
end

-- ============================================================
-- УПОР В ПРЕДЕЛ ЗАПРЕЩАЕТ ДЕЙСТВИЕ ТОЛЬКО ПРИ ЦЕЛОМ ПРЕДЕЛЕ
--
-- Правило «пробежал всё — либо действуй, либо переводи дух» держится на
-- том, что предел БОЛЬШОЙ: двенадцать метров за круг выхаживаются
-- намеренно, и отдать за них действие — осознанный размен.
--
-- Под замедлением размена нет. Предел падает до трёх (ниже пол не
-- пускает), а три метра игрок проходит, просто переступив с ноги на
-- ногу в чужой ход, — и приходит на свой уже исчерпанным. Ход
-- закрывался сам, ещё до того как игрок успевал что-либо решить:
-- достаточно сильное замедление буквально выключало персонажа из сцены,
-- и снять его было нечем, потому что действовать он тоже не мог.
--
-- ЭТО НАКАЗАНИЕ ДВАЖДЫ ЗА ОДНО. Замедление уже отняло подвижность;
-- отнимать сверх того ещё и действие — значит превращать любой
-- контроль в оглушение, которого никто не накладывал.
--
-- Поэтому упор запрещает действие ТОЛЬКО когда предел не срезан. Срезал
-- его кто угодно — замедление, Ведущий — упор остаётся упором для
-- передвижения (счётчик красный, метры кончились), но ход и действие
-- при игроке.
--
-- ПОДВИЖНОСТИ ЭТО НЕ ДОБАВЛЯЕТ НИ МЕТРА: метры как кончились, так и
-- кончились. Меняется одно — замедленный остаётся участником сцены.
-- ============================================================

--- Мешает ли упор в предел ДЕЙСТВОВАТЬ (а не только идти).
--- Это спрашивают каст, пикер порядка и авто-пропуск; сам счётчик и его
--- цвет по-прежнему смотрят на IsExhausted.
function SB.Movement.BlocksAction()
    -- ЗАМОК ДЕЙСТВОВАТЬ НЕ МЕШАЕТ, и это главное, что отличает его от
    -- упора. Вне своего хода действовать и так нельзя — этим заведует
    -- очередь, — а запрет отсюда сработал бы ровно в тот миг, когда ход
    -- наконец дойдёт: игрок получил бы свой черёд уже запертым. Метров
    -- замок всё равно не даёт: шаг в чужой ход стоит усталости
    -- (см. врезку в шапке файла).
    if SB.Movement.IsPinned() then return false end
    if not SB.Movement.IsExhausted() then return false end
    -- Предел срезан — значит подвижность уже отобрали, и действие сверху
    -- отбирать нечем.
    local cap = SB.Movement.GetCap()
    if cap ~= NO_LIMIT and cap < SB.Movement.GetDefaultCap() then
        return false
    end
    return true
end

-- ============================================================
-- ЗДЕСЬ БЫЛ АВТОМАТИЧЕСКИЙ ПРОПУСК ХОДА
--
-- Выбранный запас закрывал ход сам: игрок с нулём метров всё равно не
-- мог действовать, и держать такой ход открытым казалось бессмысленным.
--
-- С НЕПОДВИЖНЫМ СВОИМ ХОДОМ ЭТО ПРЕВРАТИЛОСЬ БЫ В МАШИНУ ПРОПУСКОВ:
-- запас обнуляется у КАЖДОГО и КАЖДЫЙ круг (см. врезку в шапке файла), и
-- ход уходил бы раньше, чем игрок успевал поднести курсор к кнопке.
--
-- Но и до правила о своём ходе пропуск отнимал ходы на ровном месте:
-- достаточно было выходить метры в чужой черёд — вдоль стены, обходя
-- союзника, — и свой ход исчезал, не начавшись. Поэтому правило снято
-- целиком, а не оговорено режимом. Упор в предел по-прежнему запрещает
-- ДЕЙСТВИЕ (см. BlocksAction), и этого довольно: ход остаётся при
-- игроке, а закрыть его он волен сам.
-- ============================================================

--- Можно ли сейчас совершить действие.
---
--- МОЛЧА. Ни при упоре в предел, ни при отказе в чат ничего не пишется:
--- состояние и так видно постоянно — бейдж передвижения в шапке краснеет
--- и показывает 12/12, — а строка в чате повторялась бы на каждый шаг
--- вдоль стены и на каждое нажатие «Применить».
---
--- Отдельной функцией, потому что точек входа в «действие» две:
--- SB.Logic.ConfirmCast (обычный каст) и SB.ActiveEffects.Use (клик по
--- активному эффекту). Вторая СПИСЫВАЕТ ПРИМЕНЕНИЕ ДО каста, так что
--- проверять предел уже внутри ConfirmCast поздно — заряд потока сгорел
--- бы впустую на каждом отказе.
--- @return boolean
function SB.Movement.CheckCanAct()
    -- BlocksAction, а не IsExhausted: срезанный предел отнимает метры,
    -- но не действие (см. врезку выше). Это ГЛАВНЫЕ ворота — через них
    -- проходит и обычный каст, и клик по эффекту.
    return not SB.Movement.BlocksAction()
end

-- ============================================================
-- УСТАЛОСТЬ: БЕГ СВЕРХ ПРЕДЕЛА СТОИТ ЗДОРОВЬЯ
--
-- Предел сам по себе запрещает только действие: выбрал двенадцать
-- метров — либо стой, либо пропускай ход. Убежать при этом можно было
-- куда угодно и бесплатно, и «отступить на тридцать метров» ничем не
-- отличалось от «отступить на двенадцать».
--
-- Правило: каждые Config.MoveFatigueStep метров, пройденные ПОСЛЕ
-- упора, стоят Config.MoveFatigueDamage здоровья.
--
-- ЧТО ЗДЕСЬ НЕОЧЕВИДНО:
--
--  • Перебег считается ОТДЕЛЬНЫМ счётчиком (moveOver), а основной путь
--    по-прежнему зажат капом. Иначе в шапке появилось бы «118/12», а
--    решение показывать упор как 12/12 принималось не просто так.
--
--  • Заплаченное запоминается (moveFatiguePaid), а не вычисляется из
--    остатка. Счётчик тикает каждый кадр, и без памяти о выплаченном
--    один и тот же метр списывал бы здоровье снова и снова.
--
--  • Предупреждение приходит РАНЬШЕ первого урона — в момент упора.
--    Бег в игре это примерно 6.4 метра в секунду: при шаге в три метра
--    расплата идёт дважды в секунду, и без окна «остановись сейчас»
--    правило работало бы как ловушка, а не как цена.
--
--  • Павший больше не устаёт. Ноль здоровья — это выход из сцены, и
--    добивать лежащего за то, что его тащат, незачем.
--
--  • Сообщения копятся и уходят пачкой (FATIGUE_REPORT). Две строки в
--    секунду про −1 ХП — это не отчёт, а помеха.
-- ============================================================

-- Как часто отчитываться об усталости в чат, секунд.
local FATIGUE_REPORT = 1.0

local fatigueWarned  = false   -- предупреждали ли в этом ходу
local fatiguePending = 0       -- ХП, о которых ещё не отчитались
local fatigueDueAt   = 0

--- Действует ли усталость сейчас.
---
--- ПО УМОЛЧАНИЮ — ДА, и отдельной галочки «включить усталость» нет.
--- Это не украшение, а половина смысла предела: без цены за бег ПвП
--- сводится к «убегаю и не отвечаю», и никакая очередь ходов этого не
--- ловит. Выключается вместе с самим пределом, одним решением Ведущего
--- на сцену (см. TO.SetMoveFree), — потому что «предела нет» и «за бег
--- сверх предела платят» противоречили бы друг другу.
function SB.Movement.IsFatigueOn()
    if SB.TurnOrder then
        -- В свободной игре метры не копятся вовсе (см. ShouldCount), и
        -- цены за бег там быть не может: персонаж просто идёт по миру.
        if SB.TurnOrder.IsActive and not SB.TurnOrder.IsActive() then return false end
    end
    -- Снятый предел — хоть Ведущим на сцену, хоть собственным побегом —
    -- отменяет и плату за бег: «предела нет» и «за бег сверх предела
    -- платят» противоречили бы друг другу (см. SB.Movement.IsUnlimited).
    if SB.Movement.IsUnlimited() then return false end
    -- СЕКУНДЫ НА ВХОДЕ В РЕЖИМ НЕ СТОЯТ НИЧЕГО. Метры в них считаются
    -- как обычно — и прощаются целиком, когда окно закроется, — но
    -- платить за них здоровьем не за что: персонаж шёл, когда Ведущий
    -- нажал кнопку, и объявление ещё не прочитал.
    if SB.Movement.InEntryGrace() then return false end
    return true
end

--- Сколько метров пройдено СВЕРХ предела в этом ходу.
function SB.Movement.GetOverrun()
    local d = db()
    return (d and tonumber(d.moveOver)) or 0
end

--- Отчёт об усталости — МЕСТНЫЙ, не в рассылку.
---
--- Само изменение здоровья группа и так увидит: PM.GrantHealth шлёт
--- STATUS_CHANGED. А вот строка про метры интересна только тому, кто
--- бежит, и в рейде десяток бегущих превратил бы её в десяток сообщений
--- в секунду — ровно тот шторм, от которого канал уводили.
local function FlushFatigueReport()
    if fatiguePending <= 0 then return end
    local lost = fatiguePending
    fatiguePending = 0
    print(SB.Theme.MSG_TAG .. "[Spellbreaker]|r: " .. SB.Theme.MSG_BAD ..
        string.format("усталость: -%d ХП|r", lost) .. SB.Theme.MSG_BODY ..
        string.format(" (%.0f м сверх предела).|r", SB.Movement.GetOverrun()))
end

--- Записать метры, пройденные сверх предела, и взять за них плату.
---
--- Отдельной функцией, а не строчками внутри шагомера: сюда смотрит
--- прогон без игры (кадров там нет, а правило проверить надо), и здесь
--- же видно всё правило целиком.
--- @param meters number  сколько метров прошли сверх предела за этот кадр
--- @return number  сколько ХП снято этим вызовом
function SB.Movement.AddOverrun(meters)
    meters = tonumber(meters) or 0
    if meters <= 0 or not SB.Movement.IsFatigueOn() then return 0 end

    local d = db()
    if not d then return 0 end

    local PM = SB.PlayerModel
    -- Павший не устаёт: ноль здоровья — уже выход из сцены.
    if PM and PM.IsDowned and PM.IsDowned() then return 0 end

    local cfg   = SB.Data.Config or {}
    local step  = math.max(0.1, tonumber(cfg.MoveFatigueStep) or 3)
    local per   = math.max(0, tonumber(cfg.MoveFatigueDamage) or 1)

    local over = (tonumber(d.moveOver) or 0) + meters
    d.moveOver = over

    local paid  = tonumber(d.moveFatiguePaid) or 0
    local due   = math.floor(over / step)
    local owed  = due - paid
    if owed <= 0 or per <= 0 then return 0 end

    d.moveFatiguePaid = due
    local lost = owed * per
    -- «self»: усталость — плата за свой же бег, а не чужой удар. Пока
    -- она шла общим каналом урона, ею снимался любой эффект с
    -- breakOn.damaged — полиморф стоил одной единицы здоровья и одного
    -- шага за предел (см. врезку у PM.GrantHealth).
    if PM and PM.GrantHealth then PM.GrantHealth(-lost, "self") end

    fatiguePending = fatiguePending + lost
    local now = GetTime()
    if now >= fatigueDueAt then
        fatigueDueAt = now + FATIGUE_REPORT
        FlushFatigueReport()
    end
    return lost
end

--- Предупредить, что дальше начинается плата. Один раз за ход.
local function WarnFatigue()
    if fatigueWarned or not SB.Movement.IsFatigueOn() then return end
    fatigueWarned = true
    if not UIErrorsFrame then return end
    -- ПРИЧИНУ НАЗЫВАЕМ ПРАВИЛЬНО. Под замком предел не «выбран» — он
    -- просто не ваш в этом окне, и игрок, прочитавший «выбран», пошёл бы
    -- искать, куда делись метры, которых он не тратил.
    UIErrorsFrame:AddMessage(
        SB.Movement.IsPinned()
            and "Не ваш ход — передвижения нет: шаг пойдёт в усталость."
            or  "Предел передвижения выбран — дальше пойдёт усталость.",
        1, 0.5, 0.2, 1, 4)
end

-- ============================================================
-- ЧЕРТА ЗАПАСА — КОНЕЦ СВОЕГО ХОДА
--
-- Ключ — «сцена:круг», в котором свой ход закрылся, и лежит он в
-- сохранёнке рядом с самим путём. Не в памяти: иначе /reload после
-- своего хода не узнал бы, что черта уже пройдена, и стёр бы метры,
-- набранные после действия, — бесплатный сброс одной командой.
--
-- Сброс ровно один на ход: отметка «походил» приходит дважды — своя
-- сразу и от Ведущего пакетом, — и второй раз стёр бы то, что пройдено
-- в промежутке.
-- ============================================================

--- Свой ход закрыт: запас на следующий считается отсюда.
--- @param key string  «сцена:круг», в котором ход закрылся
function SB.Movement.NoteTurnClosed(key)
    local d = db()
    if not d or not key then return end
    if d.moveTurnKey == key then return end
    d.moveTurnKey = key
    SB.Movement.ResetDistance()
end

--- Круг сменился. Если свой ход в прошедшем круге так и не закрылся —
--- персонажа не было в очереди (подключился посреди круга), — черты не
--- было вовсе, и путь копился бы без конца. Тогда черта — сам конец
--- круга. У того, чей ход закрылся, здесь ничего не происходит.
--- @param prevKey string  «сцена:круг» прошедшего круга
function SB.Movement.NoteRoundPassed(prevKey)
    local d = db()
    if not d or not prevKey then return end
    if d.moveTurnKey == prevKey then return end
    d.moveTurnKey = prevKey
    SB.Movement.ResetDistance()
end

--- Обнулить пройденный путь. Черта хода (см. выше), отдых, смена режима.
---
--- ЗАМОК ЭТА ФУНКЦИЯ НЕ ТРОГАЕТ, и это важно. Путь стирается из
--- полудюжины мест — отдых, конец окна на входе, смена режима, — и
--- почти всюду вопрос «чей сейчас ход» к ним не относится: стёртый
--- заодно флаг означал бы, что персонаж в чужой ход внезапно получил
--- полный предел. Снимает замок ровно одно: SB.Movement.FillBudget,
--- то есть «дошла очередь».
function SB.Movement.ResetDistance()
    local d = db()
    if not d then return end
    -- Долг усталости снимается вместе с путём: перебег — это событие
    -- ОДНОГО хода, и тащить его в следующий значило бы брать плату
    -- дважды за одни и те же метры.
    fatigueWarned = false
    FlushFatigueReport()
    local hadOver = (d.moveOver or 0) ~= 0 or (d.moveFatiguePaid or 0) ~= 0
    d.moveOver, d.moveFatiguePaid = 0, 0
    if (d.moveDistance or 0) == 0 and not hadOver then return end
    d.moveDistance = 0
    SB.Events.Fire(SB.E.MOVEMENT_CHANGED)
end

--- Задать личный кап (ГМ-правка).
--- @param value number|nil  метры; nil — вернуть значение по умолчанию,
---        отрицательное — снять предел совсем (SB.Movement.NO_LIMIT).
function SB.Movement.SetCap(value)
    local d = db()
    if not d then return end
    if value == nil then
        d.moveCap = nil
    else
        local v = tonumber(value) or 0
        d.moveCap = (v < 0) and NO_LIMIT or v
    end
    SB.Movement.InvalidateCapCache()
    SB.Events.Fire(SB.E.MOVEMENT_CHANGED)
end

-- ============================================================
-- ШАГОМЕР
-- ============================================================

local tracker = CreateFrame("Frame")
local sinceNotify = 0
-- nil, а не false: первое вычисление только ЗАПОМИНАЕТ состояние, не
-- считая его переходом. Иначе персонаж, вышедший из игры уже упёртым в
-- предел (moveDistance переживает релог), слал бы MOVEMENT_CHANGED прямо
-- на экране загрузки, когда шапки ещё нет.
local wasExhausted = nil
-- Сколько было пройдено на прошлом обновлении шапки (см. Step).
local shownDistance = nil

--- Считать ли перемещение прямо сейчас.
--- Полёт по маршруту и посмертный забег бегом персонажа не являются:
--- на такси игрок пролетает сотни метров, ничего не решая, а призрак
--- вообще не участвует в сцене. Оба случая иначе выбирали бы кап
--- мгновенно и запирали игрока сразу после воскрешения.
local function ShouldCount()
    -- ПРЕДЕЛ ПЕРЕДВИЖЕНИЯ СУЩЕСТВУЕТ ТОЛЬКО В ПОШАГОВОМ РЕЖИМЕ. Ход
    -- состоит из перемещения и действия, а «ход» есть только там, где
    -- время стоит и двигается очередью (см. Core/TurnOrder.lua). В
    -- свободной игре персонаж просто ходит по миру, и считать ему метры
    -- незачем — счётчик всё равно упирался бы в предел на первой же
    -- пробежке через город и запирал бы касты до пропуска хода.
    if SB.TurnOrder and not SB.TurnOrder.IsActive() then return false end
    if UnitOnTaxi and UnitOnTaxi("player") then return false end
    if UnitIsDeadOrGhost and UnitIsDeadOrGhost("player") then return false end
    return true
end

-- Кап пересчитывается не каждый кадр. Внутри он идёт через
-- SB.Skills.GetEffective и SB.ActiveEffects.GetMod, а те перебирают все
-- висящие эффекты — на 60 кадрах в секунду это ощутимая работа ради
-- числа, которое меняется от силы раз в несколько ходов. Обновляем его
-- в том же ритме, что и событие для шапки (пять раз в секунду), а на
-- прямые правки (/sb move, эффекты) сбрасываем кэш явно.
local cachedCap = nil

function SB.Movement.InvalidateCapCache()
    cachedCap = nil
end

-- ============================================================
-- СКОЛЬКО ПЕРСОНАЖ ПРОШЁЛ НА САМОМ ДЕЛЕ
-- ============================================================

--- Собственная скорость либо скорость того, кто везёт.
--- @return number  ярдов в секунду
local function CurrentSpeed()
    local own = GetUnitSpeed("player") or 0
    if own > 0 then return own end
    -- Ноль у себя — возможно, везут. У штатного транспорта скорость
    -- спрашиваем прямо, у него свой юнит-токен.
    if UnitInVehicle and UnitInVehicle("player") then
        return (GetUnitSpeed("vehicle") or 0)
    end
    return 0
end

-- Откуда мерили в прошлый раз. nil — точки отсчёта нет: первый кадр,
-- смена зоны, выход из подземелья.
local lastX, lastY, lastInst

--- Сколько метров прибавили координаты с прошлой сверки.
---
--- ТОЛЬКО КОГДА СКОРОСТЬ МОЛЧИТ. Пока GetUnitSpeed отвечает, он и точнее,
--- и дешевле; координаты нужны ровно для случая «везут способом, о
--- котором игровой API не знает».
---
--- БОЛЬШИЕ СКАЧКИ ОТБРАСЫВАЕМ. Телепорт, портал, воскрешение у духа и
--- загрузочный экран сдвигают персонажа на сотни ярдов мгновенно —
--- засчитать это как пробежку значило бы выбрать предел за один кадр и
--- запереть человека сразу после перехода. Порог берём заведомо выше
--- любой настоящей скорости: даже верхом за пятую долю секунды столько
--- не проехать.
local MAX_STEP_YARDS = 40

-- Была ли собственная скорость с прошлой сверки координат. Сдвиг по
-- координатам засчитывается, только если НЕ была ни разу: иначе в него
-- входит шаг, уже посчитанный по скорости (см. SB.Movement.Step).
local spedSinceSample = false

local function PositionStep()
    if not UnitPosition then return 0 end
    local y, x, _, inst = UnitPosition("player")
    if not x or not y then
        -- Подземелье или поле боя: координат нет вовсе. Сбрасываем
        -- точку отсчёта, иначе на выходе оттуда первый же замер дал бы
        -- разницу через полмира.
        lastX, lastY, lastInst = nil, nil, nil
        return 0
    end

    local px, py, pinst = lastX, lastY, lastInst
    lastX, lastY, lastInst = x, y, inst

    if not px or pinst ~= inst then return 0 end

    local d = math.sqrt((x - px) ^ 2 + (y - py) ^ 2)
    if d > MAX_STEP_YARDS then return 0 end
    return d
end

--- ОДИН КАДР ШАГОМЕРА.
---
--- ВЫНЕСЕН ИЗ ЗАМЫКАНИЯ OnUpdate НАРОЧНО. Пока весь счёт жил внутри
--- обработчика кадра, проверить его было нечем: прогон кадров не
--- рисует, и до кода внутри OnUpdate не достучаться. А считает он
--- механику, а не оформление — предел передвижения, усталость, упор, —
--- и молчаливая ошибка здесь стоит игроку хода.
---
--- Обработчик кадра ниже теперь состоит из одной строки: вызова этой
--- функции. Больше в нём ничего и не должно быть.
--- @param elapsed number  секунд с прошлого кадра
function SB.Movement.Step(elapsed)
    sinceNotify = sinceNotify + elapsed
    local due   = (sinceNotify >= NOTIFY_INTERVAL)
    local speed = CurrentSpeed()
    if speed > 0 then spedSinceSample = true end

    -- САМЫЙ ЧАСТЫЙ КАДР: персонаж стоит, до опроса ещё далеко. Выходим
    -- на двух сравнениях, не трогая ни сохранёнки, ни навыки, ни эффекты.
    if speed <= 0 and not due then return end

    local d = db()
    if not d then return end

    if due or cachedCap == nil then cachedCap = SB.Movement.GetCap() end
    local cap    = cachedCap
    local walked = tonumber(d.moveDistance) or 0

    -- УПЁРСЯ — ПУТЬ БОЛЬШЕ НЕ РАСТЁТ. Он зажимается ровно капом и там
    -- остаётся: сверх предела число ничего не решает (запрет один и тот
    -- же), зато на экране 118/12 выглядело бы сломанным счётчиком.
    --
    -- Метры при этом не выбрасываются: если Ведущий включил усталость,
    -- они уходят в отдельный счётчик перебега и стоят здоровья (см.
    -- SB.Movement.AddOverrun). Выключена — AddOverrun выходит на первой
    -- строке, и кадр остаётся ровно таким же дешёвым, как был.
    local limited = (cap ~= NO_LIMIT)
    -- ЗАМОК ЧУЖОГО ХОДА — ТОТ ЖЕ УПОР ДЛЯ ШАГОМЕРА. Иначе правило
    -- «ходишь только в свой ход» держалось бы на честном слове: аддон
    -- не может отнять клавиши, и без цены за шаг можно было бы уходить
    -- в каждый чужой ход бесплатно (см. врезку в шапке).
    local capped  = (limited and walked >= cap) or SB.Movement.IsPinned()

    -- ЗАПАСНОЙ ЗАМЕР ПО КООРДИНАТАМ — раз в NOTIFY_INTERVAL и только
    -- когда скорость молчит. Пять раз в секунду вместо шестидесяти:
    -- дешевле, а для шагомера пятая доля секунды — не разница.
    --
    -- Считаем ЗДЕСЬ, а не в общей ветке ниже: там путь прибавляется от
    -- скорости за кадр, а координаты дают уже готовые метры за интервал,
    -- и умножать их на elapsed было бы неверно.
    -- КОРОТКИЙ ШАГ НЕ СЧИТАЕТСЯ ДВАЖДЫ. Раньше условие было «скорость
    -- молчит в кадре опроса», и шаг, начавшийся и кончившийся между двумя
    -- опросами, засчитывался дважды: кадрами по скорости и ещё раз
    -- сдвигом координат, когда к опросу игрок уже стоял. Микрошаг давал
    -- два-три метра вместо одного. Теперь сдвиг засчитывается, только
    -- если скорость молчала ВЕСЬ интервал — то есть персонажа и правда
    -- везли, а не он шёл.
    local carried = 0
    if due then
        local step = PositionStep()
        if speed <= 0 and not spedSinceSample then
            carried = step * YARDS_TO_METERS
        end
        spedSinceSample = false
    end

    if carried > 0 and ShouldCount() then
        if not capped then
            walked = walked + carried
            if limited and walked > cap then
                local over = walked - cap
                walked = cap
                SB.Movement.AddOverrun(over)
            end
            d.moveDistance = walked
            capped = (limited and walked >= cap)
            if capped then WarnFatigue() end
        else
            SB.Movement.AddOverrun(carried)
        end
    end

    if speed > 0 and ShouldCount() then
        -- GetUnitSpeed отдаёт ярды в секунду; текущая скорость — первый
        -- возврат. Накапливаем КАЖДЫЙ кадр (а не раз в 0.2 с): остановка
        -- в середине интервала иначе засчитывалась бы как полный интервал
        -- бега, и путь врал бы в большую сторону на метр с лишним за
        -- каждый рывок.
        local step = speed * elapsed * YARDS_TO_METERS

        if not capped then
            walked = walked + step
            if limited and walked > cap then
                -- Часть шага, ушедшая ЗА предел, — это уже усталость, а
                -- не путь. Без этого кусочка первый метр перебега всегда
                -- терялся бы, а на быстром беге терялось бы до трёх.
                local over = walked - cap
                walked = cap
                SB.Movement.AddOverrun(over)
            end
            d.moveDistance = walked
            capped = (limited and walked >= cap)
            if capped then WarnFatigue() end
        else
            -- УПЁРСЯ. Путь больше не растёт (см. врезку ниже), но метры
            -- продолжают считаться — теперь в долг усталости. Правило
            -- выключено — AddOverrun выйдет на первой строке, и кадр
            -- останется таким же дешёвым, как был.
            SB.Movement.AddOverrun(step)
        end
    end

    if not due then return end
    sinceNotify = 0

    -- Событие шлём только когда есть что показывать: либо игрок движется
    -- и счётчик ещё растёт, либо состояние «упёрся» только что
    -- изменилось. Иначе стоящий на месте (или уже упёртый) персонаж
    -- перерисовывал бы шапку пять раз в секунду вечно.
    if wasExhausted == nil then
        wasExhausted = capped
        return
    end
    -- ОБНОВЛЯЕМ, ЕСЛИ ПУТЬ ВЫРОС, А НЕ ЕСЛИ ИГРОК ДВИЖЕТСЯ ПРЯМО СЕЙЧАС.
    -- Было «скорость больше нуля в кадре опроса». Микрошаг короче пятой
    -- доли секунды в счётчик попадал, а к опросу игрок уже стоял — и
    -- шапку никто не просил перерисоваться. Метры копились невидимо, по
    -- метру-два, и всплывали разом, когда какой-нибудь шаг случайно
    -- приходился на опрос: «прошёл почти весь ящик на нуле, и вдруг 2.2».
    -- Сравнение с показанным ловит любой рост — бегом, координатами,
    -- сколь угодно коротким шагом.
    local walkedNow = tonumber(d.moveDistance) or 0
    local grew = (shownDistance == nil) or (walkedNow ~= shownDistance)
    if grew or capped ~= wasExhausted then
        wasExhausted  = capped
        shownDistance = walkedNow
        SB.Events.Fire(SB.E.MOVEMENT_CHANGED)
    end

end

tracker:SetScript("OnUpdate", function(_, elapsed)
    SB.Movement.Step(elapsed)
end)

-- ============================================================
-- ОПИСАНИЕ ДЛЯ ТУЛТИПА — один раз здесь, чтобы UI не собирал текст
-- правил у себя (см. UI/MainFrame.lua).
-- ============================================================
SB.Data.Tooltips = SB.Data.Tooltips or {}
SB.Data.Tooltips["movement"] = {
    title = "Передвижение за круг",
    lines = {
        -- ПЯТЬ СТРОК СВЁРНУТЫ В ДВЕ. Было по строке на каждое правило:
        -- где считается, что показывает, что бывает на пределе, когда
        -- обнуляется, что даёт пропуск. Порознь всё это верно, вместе —
        -- абзац, который на весу под курсором не читают.
        --
        -- Свёрнуто по смыслу: первая строка отвечает «когда это вообще
        -- работает», вторая — «что будет, если упрусь». Разбивка предела
        -- ниже осталась как была: она про числа, а не про правила.
        "Считается только в пошаговом режиме; счётчик обнуляется с новым кругом — один раз и у всех сразу.",
        "Запас на ВЕСЬ круг: тратьте когда угодно, хоть до своего хода, хоть после. Кончился — остаётся окончить ход.",
        -- РАЗБИВКА ПРЕДЕЛА, а не пересказ правил. Число в шапке
        -- складывается из четырёх источников сразу, и «почему у меня 21,
        -- а у него 12» иначе не выяснить: расовый и классовый рычаги не
        -- показаны больше нигде, кроме подсказки портрета, а «Спринт»
        -- меняет число на ходу и выглядит как сбой счётчика.
        --
        -- Слагаемые перечисляем ТОЛЬКО ненулевые: строка «раса +0» ничего
        -- не сообщает, а место занимает.
        function()
            local cfg   = SB.Data.Config or {}
            local base  = tonumber(cfg.MoveCap) or 12
            local parts = { string.format("база %d", base) }

            local athletics = (SB.Skills and SB.Skills.GetAthleticsMoveBonus
                and SB.Skills.GetAthleticsMoveBonus()) or 0
            if athletics ~= 0 then
                parts[#parts + 1] = string.format("«Атлетика» %+d", athletics)
            end

            local soft = (SB.Data.GetSoftBonus and SB.Data.GetSoftBonus("moveCap")) or 0
            if soft ~= 0 then
                parts[#parts + 1] = string.format("раса и класс %+d", soft)
            end

            local pct = (SB.ActiveEffects and SB.ActiveEffects.GetMod
                and SB.ActiveEffects.GetMod("movePct")) or 0
            if pct ~= 0 then
                parts[#parts + 1] = string.format("эффекты %+d%%", pct)
            end

            local cap = SB.Movement.GetCap()
            -- Замедление упёрлось в пол — говорим об этом прямо. Иначе
            -- игрок видит «−150%» рядом с тремя метрами и считает, что
            -- счётчик врёт (см. Config.MoveCapMin).
            local floor = tonumber(cfg.MoveCapMin) or 3
            if pct < 0 and cap == floor and cap ~= NO_LIMIT then
                parts[#parts + 1] = string.format("но не ниже %d м", floor)
            end
            if cap == NO_LIMIT then
                return "Предел снят Ведущим лично для вас."
            end
            return string.format("Ваш предел: %d м (%s).", cap, table.concat(parts, ", "))
        end,
        -- Ведущий вправе снять предел на сцену (см. TO.SetMoveFree).
        -- Строка появляется только когда это действительно так: иначе
        -- она сообщала бы о правиле, которого сейчас нет.
        function()
            -- Причину называем словами: «предела нет» игрок видит и так,
            -- а вот почему его нет — только отсюда.
            if SB.PlayerModel and SB.PlayerModel.HasFled and SB.PlayerModel.HasFled() then
                return "|cFF66CCFFВы вышли из боя:|r предел не действует и усталости нет — уходите столько, сколько нужно."
            end
            if not (SB.TurnOrder and SB.TurnOrder.IsMoveFree
                    and SB.TurnOrder.IsMoveFree()) then return "" end
            return "|cFF66CCFFВедущий снял предел на этой сцене:|r метры считаются, но ничего не запрещают."
        end,
        -- Строкой-функцией: собирается в момент показа (см.
        -- SB.UI.ShowInfoTooltip). Про цену бега молчим, пока Ведущий не
        -- включил правило, — обещать урон, которого нет, так же плохо,
        -- как не предупредить о том, который есть. Пустая строка не
        -- рисуется вовсе.
        function()
            if not SB.Movement.IsFatigueOn() then return "" end
            local cfg = SB.Data.Config or {}
            return string.format(
                "|cFFFF6666Усталость:|r каждые %d м сверх предела стоят %d ХП.",
                cfg.MoveFatigueStep or 3, cfg.MoveFatigueDamage or 1)
        end,
    },
}
